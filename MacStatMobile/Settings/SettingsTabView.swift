import SwiftUI
import MacStatKit

/// Tab 4 — Settings (plan §12.1: devices & sync status, notification
/// preferences, widget configuration, theme sharing Theme tokens with the
/// Mac app). Placeholder only; see `HistoryTabView`'s doc comment for why
/// the stub exists rather than an omitted tab.
struct SettingsTabView: View {
    var body: some View {
        StubTabContent(
            title: "Settings",
            systemImage: "gearshape",
            message: "Devices & sync status, notifications, widget configuration, and theme picking are built separately."
        )
    }
}
