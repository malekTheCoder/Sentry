import Foundation

// Placeholder stdio MCP server binary. Real implementation lands in Phase 7
// (plan §13) — talks to MacStat.app over XPC, exposes the tool surface
// defined there, plus the `macstat` CLI-facing pieces from §21.
print("MacStatMCP — placeholder, not yet implemented (Phase 7)")
